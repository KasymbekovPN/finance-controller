import { NOTIFICATION } from "../../sconst/notification";

const state = {};

const getters = {};

const actions = {
	[NOTIFICATION.ERROR]: ({}) => {
		//< impl it
	}
};

const mutations = {};

export default {
	state,
	getters,
	actions,
	mutations
};
