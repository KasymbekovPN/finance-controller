import { TAG } from "../../sconst/tag";

const state = {};

const getters = {};

const actions = {
	[TAG.CREATED]: ({}) => {
		//< impl it
	}
};

const mutations = {};

export default {
	state,
	getters,
	actions,
	mutations
};
